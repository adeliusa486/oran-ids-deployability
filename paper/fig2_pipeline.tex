% ---------------------------------------------------------------------------
%  FIGURE 2 -- Experimental pipeline: one trained detector, four protocols.
%
%  Rebuilt in TikZ from figures/source/fig2_pipeline_raw.jpeg. The pictograms
%  are the originals (scripts/extract_figure_icons.py); every word is set in
%  LaTeX so the figure text is selectable and in the body font.
%
%  Corrections against the source render:
%   * it carried three layout captions -- "LEFT SPINE", "RIGHT SIDE",
%     "RUNTIME / LATENCY ARM". Those were instructions to the renderer, not
%     labels, and they are removed along with the title.
%   * it labelled a stage "measured latency" and drew an offered-load versus
%     queueing-delay curve. No load sweep was performed and the latency is
%     emulated, so the runtime panel now reports only the stage decomposition
%     that was measured, and is marked EMULATED.
% ---------------------------------------------------------------------------
\providecommand{\ficontwo}[2]{%
  \raisebox{-0.45\height}{\includegraphics[height=#2]{../figures/icons/#1.png}}}

\begin{tikzpicture}[
  font=\scriptsize,
  every node/.style={inner sep=1pt, outer sep=0pt},
  spine/.style ={draw=black!60, fill=black!3, rounded corners=1.6pt,
                 align=left, inner sep=3pt, text width=2.78cm,
                 minimum height=0.92cm},
  % the rungs gain weight as the protocol gets closer to deployment
  rungA/.style ={draw=black!42, line width=0.5pt, fill=black!1,
                 rounded corners=1.6pt, align=left, inner sep=4pt,
                 text width=5.30cm, minimum height=1.02cm},
  rungB/.style ={rungA, draw=black!58, line width=0.8pt, fill=black!4},
  rungC/.style ={rungA, draw=black!74, line width=1.05pt, fill=black!7},
  rungD/.style ={rungA, draw=black!90, line width=1.35pt, fill=black!11},
  blkd/.style  ={draw=black!42, dashed, fill=black!5, rounded corners=1.6pt,
                 align=center, inner sep=4pt, text=black!50,
                 text width=5.30cm, dash pattern=on 1.6pt off 1.4pt},
  arr/.style   ={-{Latex[length=1.6mm]}, draw=black!60, line width=0.6pt},
  arrx/.style  ={-{Latex[length=1.6mm]}, draw=black!45, dashed,
                 line width=0.5pt, dash pattern=on 1.4pt off 1.2pt},
  num/.style   ={circle, draw=black!75, fill=white, inner sep=0.5pt,
                 font=\tiny\bfseries, minimum size=3.3mm},
  cost/.style  ={font=\tiny\bfseries, text=black!80, align=left, anchor=west},
  panel/.style ={draw=black!50, fill=black!2, rounded corners=1.6pt,
                 inner sep=4pt, align=center, text width=3.9cm},
]

% ===== training spine ======================================================
\node[spine] (src) at (0,7.00)
  {\textbf{$D_A$: O-RAN testbed} \\ {\tiny Zeek flow records $+$ radio KPIs}};
\node at (1.92,7.00) {\ficontwo{src_testbed}{4.0mm}};

\node[spine] (fsp) at (0,5.30)
  {\textbf{Shared feature space} \\ {\tiny 15 concepts / 18 columns} \\ {\tiny semantic harmonisation}};
\node at (1.95,5.30) {\ficontwo{schema}{4.8mm}};

\node[spine] (spl) at (0,3.60)
  {\textbf{Group-disjoint split} \\ {\tiny \texttt{src\_ip}, 20 split seeds}};
\node at (1.98,3.60) {\ficontwo{grouplock}{4.2mm}};

\node[spine, minimum height=1.45cm] (trn) at (0,1.55)
  {\textbf{Train} \\ {\tiny 8 architectures, including 2 trivial floors} \\
   {\tiny L1 majority, stratified} \\
   {\tiny L2 logreg, tree, RF, XGB, HGB} \\
   {\tiny L3 MLP}};

\foreach \a/\b in {src/fsp, fsp/spl, spl/trn} {\draw[arr] (\a) -- (\b);}

% ===== one trained detector feeds every rung ===============================
\draw[arr, line width=1.1pt] (trn.east) -- (3.10,1.55);
\node[font=\tiny, align=center, text=black!70] at (3.10,7.52)
  {one trained detector,
   \\ four protocols};
\draw[black!60, line width=0.6pt] (3.10,1.55) -- (3.10,7.00);

% ===== the evaluation ladder ===============================================
\node[rungA] (r1) at (7.05,7.00)
  {\textbf{Held-out $D_A$}, group-disjoint \\
   {\tiny in-distribution reference: the number this literature usually reports}};
\node[rungB] (r2) at (7.05,5.30)
  {\textbf{Time-disjoint $D_A$} \\
   {\tiny train on the earliest capture runs, test on the latest; 52.96 days of real elapsed time}};
\node[rungC] (r3) at (7.05,3.60)
  {\textbf{$D_B$: 5G-NIDD} \\
   {\tiny an independently collected deployment, evaluated once; every access logged}};
\node[rungD] (r4) at (7.05,1.55)
  {\textbf{Operational analysis} \\
   {\tiny base rate $\pi$, decision threshold $\tau$, benign arrival rate $\lambda_b$, alert burden}};

\foreach \n/\y in {1/7.00, 2/5.30, 3/3.60, 4/1.55}
  {\node[num] at (3.66,\y) {\n};
   \draw[arr] (3.88,\y) -- (4.22,\y);}

% measured cost of each rung
\node[cost] at (9.95,7.00) {reference};
\node[cost] at (9.95,5.30) {$-0.27\ F_{1}$};
\node[cost, text width=1.8cm] at (9.95,3.60) {to the trivial floor};
\node[cost, text width=1.8cm] at (9.95,1.55) {PPV $0.93$ $\to$ $0.006$};

\node at (9.66,2.62) {\ficontwo{distrib}{3.6mm}};

% the target corpus is entered once, and nothing returns

% ===== the arm that was not executed =======================================
\node[blkd, minimum height=0.68cm] (blk) at (7.05,-0.30)
  {\textbf{Live Near-RT RIC} --- \textbf{NOT EXECUTED}, see Limitations};
\draw[arrx] (3.10,1.55) -- (3.10,-0.30) -- (blk.west);

% ===== runtime decomposition, emulated =====================================
\node[panel] (rt) at (14.20,6.72)
  {\textbf{Runtime decomposition} \\ {\tiny\textsc{emulated}: no RIC in the path}};

\begin{scope}[shift={(12.24,5.28)}, font=\tiny]
  \node[draw=black!55, fill=black!3, rounded corners=1.2pt, minimum width=0.86cm,
        minimum height=0.58cm, align=center] (s0) at (0.48,0) {E2 \\ indic.};
  \node[draw=black!88, fill=black!14, rounded corners=1.2pt, minimum width=1.18cm,
        minimum height=0.58cm, align=center, very thick] (s1) at (1.72,0) {feature \\ constr.};
  \node[draw=black!55, fill=black!3, rounded corners=1.2pt, minimum width=0.86cm,
        minimum height=0.58cm, align=center] (s2) at (2.96,0) {model \\ infer.};
  \node[draw=black!55, fill=black!3, rounded corners=1.2pt, minimum width=0.86cm,
        minimum height=0.58cm, align=center] (s3) at (3.96,0) {control \\ encode};
  \draw[arr] (s0) -- (s1); \draw[arr] (s1) -- (s2); \draw[arr] (s2) -- (s3);
\end{scope}

\node[align=center, font=\tiny, text=black!80, text width=4.0cm] at (14.20,4.10)
  {$t_{\mathrm{ind}} + t_{\mathrm{feat}} + t_{\mathrm{inf}} + t_{\mathrm{act}}$ \\[2pt]
   \textbf{feature construction dominates} \\
   0.058\,ms per flow, against 1--25\,$\mu$s of inference in a deployed
   implementation};

\node[panel, text width=3.85cm] at (14.20,1.72)
  {\textbf{How to read this figure} \\[1.5pt]
   {\tiny Every step down the ladder is closer to what a deployment
   experiences, and every step costs more than the choice of architecture
   does. Rung~1 alone does not establish deployment behaviour.}};

\end{tikzpicture}
